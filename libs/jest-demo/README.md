# jest-demo

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build jest-demo` to build the library.

## Running unit tests

Run `nx test jest-demo` to execute the unit tests via [Jest](https://jestjs.io).
